name: zemato
description: Zemato food delivery app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^17.5.0
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  firebase_storage: ^13.4.6
  cached_network_image: ^3.3.1
  google_fonts: ^8.2.1
  geolocator: ^14.0.3
  geocoding: ^5.0.0
  google_maps_flutter: ^2.6.0
  flutter_rating_bar: ^4.0.1
  intl: ^0.20.3
  shimmer: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
